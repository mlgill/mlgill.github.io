{%- comment -%}
Publications section using jekyll-scholar with papers.bib
Shows selected publications marked with selected={true}
Uses simplified cv_bib template for cleaner CV formatting
{%- endcomment -%}
<ul class="card-text font-weight-light list-group list-group-flush">
  {% bibliography --template cv_bib --query @*[selected=true] %}
</ul>
